import 'package:flutter/material.dart';

class DescView extends StatelessWidget {
  const DescView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
